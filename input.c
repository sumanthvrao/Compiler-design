#include<stdio.h>
//single line comment

int copy = 80*10;
int glob1;
int fun();

int main()
{
	func(f);
	local = 1;
	if(copy > 10 )
	{
		int lhs = copy;
	}
	do
	{
		int lhs = rhs;
	}while(rhs > 2);

	if(local==1)
	{
		if(copy!=0)
		{
			int new=19;

		}
	}

	int copy2 = copy;
	// All undeclared variables initialized to 0.
	int glob2;
}
/*This function sets the
value of temp to 120*/
int fun()
{
	int temp=120;
}

$